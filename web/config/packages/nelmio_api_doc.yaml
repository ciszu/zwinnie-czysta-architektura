nelmio_api_doc:
    documentation:
        info:
            title: Mozartify
            description:
            version: 1.0.0
        host: localhost:3001
        schemes: [http]
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc) # Accepts routes under /api except /api/doc